public interface Counter {

    int countWords(String sentence); //counts the words of the sentences.
    int countLetters(String sentence); //counts the letters within each sentence.
    int getLength(String sentence); //counts the sentences.


}
